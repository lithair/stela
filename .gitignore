/target
/data
.env
/.probatum/runs
/.cargo

# The compose bind mount, generated by `stela new`
/blog

# Browser-test dependency, installed into the workspace by the playwright container
/node_modules
/package-lock.json
/test-results
